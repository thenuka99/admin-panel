function omitBy(input, predicate) {
    var output = {};
    for (var key in input) {
        if (!predicate(input[key], key)) {
            output[key] = input[key];
        }
    }
    return output;
}

export { omitBy };
