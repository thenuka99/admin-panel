function pick(input, fields) {
    var output = {};
    fields.forEach(function (field) {
        if (field in input) {
            output[field] = input[field];
        }
    });
    return output;
}

export { pick };
