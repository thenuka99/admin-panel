import type { ReactElement, ReactNode } from 'react';
import type { Position, Series } from './types';
import { Style } from './types';
export declare type TitleProps = {
    children: ReactNode | ((props: TitleProps) => ReactNode);
    textAnchor?: 'start' | 'middle' | 'end';
    className?: string;
    style?: Style;
    width?: number;
    height?: number;
    position?: Position;
    layerWidth?: number;
    layerHeight?: number;
    series?: Series[];
};
export declare function Title(props: TitleProps): ReactElement;
