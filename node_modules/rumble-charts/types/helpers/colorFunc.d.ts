import type { Colors, ColorScale } from '../types';
export declare const schemeMap: Record<string, readonly string[]>;
export declare const defaultScheme: readonly string[];
export declare const defaultSchemeName: keyof typeof schemeMap;
export declare function colorFunc(colors?: Colors): ColorScale;
