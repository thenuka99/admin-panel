import type { NormalizedSeriesProps } from '../../types';
declare type Options = {
    direction?: 'asc' | 'desc';
};
export declare function sort(props: NormalizedSeriesProps, { direction }?: Options): NormalizedSeriesProps;
export {};
