import type { NormalizedSeriesProps } from '../../types';
declare type Options = {
    normalize?: boolean;
};
export declare function stack(props: NormalizedSeriesProps, options?: Options): NormalizedSeriesProps;
export {};
