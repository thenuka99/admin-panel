'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
require('./helpers/colorFunc.js');
require('d3-shape');
require('d3-ease');
var isFunction = require('./helpers/isFunction.js');
var getCoords = require('./helpers/getCoords.js');
var isString = require('./helpers/isString.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var React__default = /*#__PURE__*/_interopDefaultLegacy(React);

function Title(props) {
    var style = props.style, className = props.className, children = props.children;
    var _a = getCoords.getCoords(props.position, props.layerWidth, props.layerHeight, props.width, props.height), x = _a.x, y = _a.y;
    return (React__default['default'].createElement("g", { className: className, transform: "translate(" + x + " " + y + ")", style: style }, isString.isString(children)
        ? React__default['default'].createElement("text", { textAnchor: props.textAnchor }, children)
        : (isFunction.isFunction(children) ? children(props) : children)));
}

exports.Title = Title;
