'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var tslib_es6 = require('../external/tslib/tslib.es6.js');
var isFunction = require('./isFunction.js');
var isObject = require('./isObject.js');
var isUndefined = require('./isUndefined.js');

function value(attribute, args) {
    if (Array.isArray(attribute)) {
        var result = void 0;
        for (var _i = 0, attribute_1 = attribute; _i < attribute_1.length; _i++) {
            var attr = attribute_1[_i];
            attr = isFunction.isFunction(attr) ? attr(args) : attr;
            if (isObject.isObject(attr) && !Array.isArray(attr) && !('_owner' in attr) && !('props' in attr)) {
                result = tslib_es6.__assign(tslib_es6.__assign({}, attr), (result || {}));
            }
            else if (!isUndefined.isUndefined(attr) && attr !== null) {
                result = attr;
                break;
            }
        }
        return result;
    }
    else {
        return isFunction.isFunction(attribute) ? attribute(args) : attribute;
    }
}

exports.value = value;
